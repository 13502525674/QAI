<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ape.apesystem.mapper.KnowledgePointMapper">

    <resultMap id="KnowledgePointResult" type="com.ape.apesystem.domain.KnowledgePoint">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="subject" column="subject"/>
        <result property="parentId" column="parent_id"/>
        <result property="difficulty" column="difficulty"/>
        <result property="level" column="level"/>
        <result property="branch" column="branch"/>
        <result property="description" column="description"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="parentName" column="parent_name"/>
        <result property="childrenCount" column="children_count"/>
    </resultMap>

    <select id="selectByBranch" resultMap="KnowledgePointResult">
        SELECT kp.*, 
               (SELECT name FROM knowledge_point WHERE id = kp.parent_id) as parent_name,
               (SELECT COUNT(*) FROM knowledge_point WHERE parent_id = kp.id) as children_count
        FROM knowledge_point kp
        WHERE kp.branch = #{branch}
        ORDER BY kp.level, kp.name
    </select>

    <select id="selectChildren" resultMap="KnowledgePointResult">
        SELECT kp.*,
               (SELECT COUNT(*) FROM knowledge_point WHERE parent_id = kp.id) as children_count
        FROM knowledge_point kp
        WHERE kp.parent_id = #{parentId}
        ORDER BY kp.name
    </select>

    <select id="selectKnowledgeGraphData" resultType="java.util.Map">
        SELECT 
            kp.id as id,
            kp.name as name,
            kp.branch as category,
            kp.parent_id as parentId,
            kp.level as level,
            kp.difficulty as difficulty
        FROM knowledge_point kp
        ORDER BY kp.level, kp.branch
    </select>

    <select id="countChildren" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM knowledge_point WHERE parent_id = #{parentId}
    </select>

</mapper>
