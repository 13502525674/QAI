# ============================================================
# 物理学术研究交流平台 — Phase 2 稳定兼容依赖
# Python 3.11 | 基于已安装环境调和版本
# ============================================================

# --- Web 框架 (已安装) ---
fastapi>=0.115.0,<0.116.0
uvicorn>=0.34.0
sse-starlette==2.2.1
python-multipart==0.0.18

# --- LLM 调用 (百炼 OpenAI 兼容接口) ---
openai>=1.58.0,<2.0.0
langchain-openai>=0.3.0,<0.4.0

# --- Agent 框架 ---
langchain>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langgraph>=0.2.50,<0.3.0
langgraph-checkpoint>=2.0.0

# --- RAG / 向量库 (pgvector + LlamaIndex, psycopg2 直连) ---
llama-index>=0.12.0
psycopg2-binary>=2.9,<3.0
pgvector>=0.3.0,<0.4.0
numpy>=1.26.0
# chromadb 已废弃 → 改用 PostgreSQL pgvector (vector 0.8.1 + HNSW 索引)
# retriever.py: psycopg2 + pgvector Python client + openai embedding

# --- 配置管理 ---
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0
python-dotenv==1.0.1

# --- JWT 认证 (已安装) ---
PyJWT>=2.4.0

# --- 工具 (已安装) ---
sympy>=1.13.0

# --- 异步 + 缓存 ---
redis>=5.0.0
httpx>=0.27.0